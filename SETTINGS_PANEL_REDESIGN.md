# Pure Hide 设置面板重组分层设计方案

> 状态：**已实施**（提交 `95be4bf`，全部视图统一场景化 `-` 后续提交）| 日期：2026-08-10
> 关联规划：`IMPROVEMENT_PLAN.md`（本设计为其中"设置面板整合优化"的深化方案）
> 设计原则：**不删除任何功能、不改变配置键与数据模型，仅重组展示层**；数据层（CONFIG）保持 16 组不变，视图层新增"场景"维度。

## 0. 实施记录与差异说明（2026-08-10）

| 项 | 设计稿 | 实施结果 |
|----|--------|----------|
| 数据层 | CONFIG 每条加 `scenes` 标记 | **改为独立 `SCENES` 常量**（视图层声明式分组，CONFIG 零改动，更安全） |
| 系统场景 | 3 个子组（含"面板管理/关于"按钮组） | **仅 1 组"入口与全局"**——导出/导入/重置已固定于工具栏与底部，不重复渲染 |
| 搜索联动 | "命中即自动切换 Tab" | 已实现：当前视图无命中时自动切至首个含命中的场景；"全部"视图下无命中即全局无命中 |
| 折叠记忆 | 持久化 | 已实现（内部键 `pure-hide-ui-state`，不进 CONFIG） |
| 滑块同步 / 单项恢复 | 设计 | 均已实现（`renderSettingItem`） |
| **"全部"视图** | 设计稿：保留 16 组功能域 | **已按用户决策变更：统一场景化渲染**（遍历 SCENES 显示全部场景与子组），旧 16 组结构彻底移除（`renderAllGroups` 已删除） |

---

## 1. 方案概述

**目标**：将现有 16 个功能域分组（98 项设置）在展示层重组为 **4 个使用场景 Tab**，解决"用户需猜测设置项归属模块"的查找问题。

**关键决策**：
- 数据层零迁移：CONFIG 的 16 组结构与所有 `pure-hide-*` 键不变，旧 `data.json` 无缝兼容。
- 视图层多归属：同一设置项可在多个场景中出现（如"正文行高"同时出现在编辑体验与阅读体验），数据层仍单一。
- **统一场景化（2026-08-10 决策变更）**："全部"Tab 按场景渲染所有子组，不再保留 16 组功能域视图；面板呈现完全统一为 L1 场景 → L2 子组 → L3 设置项三层结构。

---

## 2. 目录划分（L1 场景定义）

| 场景 Tab | 定位 | 子组数 | 设置项数（含跨场景重复） |
|----------|------|--------|--------------------------|
| **编辑体验** | 写作输入时直接相关的设置 | 7 | 约 28 |
| **阅读体验** | 阅读模式下的排版与呈现 | 5 | 约 22 |
| **界面外观** | 界面元素、材质与质感 | 6 | 约 44 |
| **系统** | 全局行为、入口与管理 | 3 | 约 10 |
| **全部** | 现有 16 组功能域视图（兼容入口） | 16 | 98（不重复） |

### 2.1 编辑体验（T1）—— 子组与设置项映射

| 子组 | 设置项（对应 CONFIG 键） | 类型 |
|------|--------------------------|------|
| 排版与间距 | 正文行高、段落间距、两端对齐、单词间距 | 滑块×4 |
| 列表与序号 | 序号宽度、序号字号、圆点字号、缩进线偏移 | 滑块×4 |
| 布局与宽度 | 编辑宽度、多列布局、多列列数 | 滑块+开关+滑块 |
| 编辑增强 | 聚焦段落、光标颜色、当前行指示、选中背景强调、括号高亮 | 开关×5 |
| 代码与行号 | 代码块背景、代码块圆角、复制按钮位置、行号显示、移除表格空行 | 下拉+滑块+下拉+下拉+开关 |
| 列表强化 | 任务样式、嵌套标记切换、列表卡片 | 下拉+开关×2 |
| 标题细节 | 标题等级标签、标题虚线、标题箭头色、水平线样式 | 开关×3+下拉 |

### 2.2 阅读体验（T2）—— 子组与设置项映射

| 子组 | 设置项 | 类型 | 备注 |
|------|--------|------|------|
| 阅读布局 | 阅读宽度独立开关、阅读宽度、衬线字体、英文断字 | 开关+滑块+开关×2 | |
| 排版增强 | 高亮样式、斜体粗体渲染、链接下划线动画、首行缩进、首字下沉、中文间距 | 下拉×2+开关×4 | |
| 阅读元素 | 引用块样式、脚注玻璃、表格换行、图片阴影、彩色背景块 | 下拉+开关×4 | |
| 阅读呈现 | 阅读视图隐藏属性、阅读标题居中 | 开关×2 | |
| 共享排版（双态） | 正文行高、段落间距、两端对齐、序号宽度、序号字号、圆点字号 | 滑块×6 | 标注"同时影响编辑模式" |

### 2.3 界面外观（T3）—— 子组与设置项映射

| 子组 | 设置项 | 类型 |
|------|--------|------|
| 界面隐藏 | 标签栏、状态栏、仓库名称、滚动条、侧边栏按钮、工具提示、文件导航按钮、搜索建议、搜索计数、操作提示、阅读属性 | 开关×11 |
| 材质与圆角 | 弹窗圆角、弹窗毛玻璃、毛玻璃透明度、媒体圆角、滚动条宽度、滚动条悬浮、毛玻璃总控 | 下拉×3+滑块+下拉+开关×2 |
| 仓库与标签页 | 切换器风格、切换器强调色、切换器悬浮动画、标签下划线、标签页高度、关闭按钮放大 | 下拉×2+开关×4 |
| 顶栏与导航 | 顶栏样式、顶栏分割线、折叠指示器、折叠动画、装订线背景、面包屑样式、文件列表圆点、隐藏根文件夹、文件图标彩色 | 下拉×5+开关×4 |
| 弹窗与菜单 | 弹窗动画、右键菜单密度 | 下拉×2 |
| 内容元素外观 | 标签尺寸、标签悬停、嵌入边框、嵌入内边距、嵌入圆角、代码块背景、代码块圆角 | 下拉×3+滑块×2+下拉+滑块 |

### 2.4 系统（T4）—— 子组与设置项映射

| 子组 | 设置项 | 类型 |
|------|--------|------|
| 入口与全局 | 设置按钮位置、界面饱和度、界面对比度、过渡动画 | 下拉+滑块×2+开关 |
| 设置管理 | 导出设置、导入设置、重置所有设置、（规划中：预设主题包） | 面板功能按钮 |
| 关于 | 查看许可证信息、支持开发者（打赏） | 命令入口 |

---

## 3. 分层结构（L1/L2/L3）

```
L1 场景 Tab（4+1）──────────────► 编辑体验 | 阅读体验 | 界面外观 | 系统 | 全部
   │
   ├─ L2 功能子组（可折叠卡片，标题 +「N 项」计数）──► 排版与间距 ▾（4 项）
   │      │
   │      └─ L3 设置项（开关 / 滑块+数值框 / 下拉）
```

**命名规范**：
- L1 场景：动宾短语（编辑体验 / 阅读体验 / 界面外观 / 系统），2–4 字，语义为"用户在做什么"。
- L2 子组：名词短语（排版与间距 / 材质与圆角），语义为"功能是什么"。
- L3 设置项：沿用现有标题（如"正文行高 (px)"），不更名以保持文档与命令一致性。

**从属关系**：L1 ⊃ L2 ⊃ L3，严格树形；同一 L3 可挂在多个 L2 下（跨场景），但每个 L2 只能属于一个 L1。

---

## 4. 分组逻辑

- **排序优先级**：场景内子组按"使用频率 × 影响面"排序——高频/全局项在前（排版 > 交互 > 细节），低频项靠后；子组内设置项沿用 CONFIG 现有顺序。
- **双态项处理**：行高、段落间距、两端对齐、列表系列同时挂载于 T1 与 T2，并在 T2 侧标注"同时影响编辑模式"的次级提示（`desc` 追加，不改数据）。
- **特殊标注**：性能敏感项（移除表格空行，`:has()`）在子组内以"性能提示"样式呈现；依赖第三方插件项（Dataview / 日历 / Canvas / Excalidraw）保持原标注。

---

## 5. 交互与导航

| 交互点 | 设计 | 反馈 |
|--------|------|------|
| 场景切换 | 顶部 Tab 栏（pill 样式，横排可换行） | 当前 Tab 背景用强调色高亮；切换即时重渲染当前场景子组 |
| 子组展开/折叠 | `<details>` 折叠卡片，默认核心子组展开、长尾子组折叠 | 展开箭头旋转 90°；"展开全部/折叠全部"工具栏按钮保持 |
| 搜索联动 | 命中即展开所在子组并**自动切换至对应场景 Tab**（跨 Tab 全局搜索） | 命中项高亮；无结果时显示"未找到匹配项"提示条 |
| 数值输入 | 滑块与数值框**实时双向同步**（修复当前滑块拖动不同步缺陷） | 拖动滑块数值框跟随更新 |
| 单项操作 | 每项右侧提供"恢复默认"小按钮 | 点击后该项值复位并轻微闪烁反馈 |
| 状态记忆 | 场景 Tab 与子组折叠状态写入内部设置键（不进 CONFIG） | 下次打开保持上次浏览位置 |
| 键盘可达 | Tab 键在 Tab/子组/设置项间移动；Enter 展开/折叠 | 焦点环清晰可见 |

---

## 6. 视觉效果

- **整体布局**（自上而下）：工具栏（搜索 + 展开/折叠 + 导入/导出）→ 场景 Tab 栏 → 子组卡片流 → 底部（已启用统计 + 重置 + 支持区）。
- **层级对比**：L1 用 pill 高亮区分选中态；L2 卡片白底 + 0.5px 边框 + 12px 圆角 + 标题 13px 加粗；L3 行内 0.5px 分隔线、12.5px 正文。三级通过背景、边框、字号递进形成视觉阶梯。
- **间距节奏**：Tab 栏与卡片区 14px；卡片间 10px；卡片内行距 8px；统一留白营造呼吸感。
- **视觉动线**：顶部搜索为第一焦点 → Tab 为第二焦点（当前场景）→ 卡片流自上而下扫描；用户找设置的路径由"猜模块"变为"选场景 → 扫子组 → 定位项"。
- **找设置路径对比**：

| 场景 | 现状（16 组） | 重组后（场景 Tab） |
|------|---------------|---------------------|
| 调"正文行高" | 猜在"排版与间距"→ 展开 → 找到 | 进入"编辑体验"→ 展开"排版与间距"→ 找到 |
| 调"引用块样式" | 猜在"统一圆角与材质"或"排版增强"→ 两处排查 | 进入"阅读体验"→ "阅读元素"→ 找到 |
| 调"毛玻璃" | 需知道"毛玻璃效果"与多个玻璃子项分散 | "界面外观"→ "材质与圆角"集中管理 |

---

## 7. 体验提升点总结

1. **查找路径缩短**：场景化心智模型与用户行为一致，平均定位步骤减少约 1–2 次点击。
2. **双态项不再遗漏**：跨场景多归属 + 提示标注，用户不会因"归属单一"漏掉设置。
3. **视觉层级清晰**：L1/L2/L3 三级视觉阶梯，长列表不再是平铺的 16 个等权分组。
4. **兼容性零风险**：数据层不变、旧配置无缝、全功能保留，"全部"Tab 兼容老用户。
5. **搜索真正跨场景**：搜索可跨 Tab 直达并自动切换场景，补足现有搜索不跨分组的短板。

---

## 8. 实施规划

| 阶段 | 内容 | 改动点 | 风险 |
|------|------|--------|------|
| S1 数据层 | CONFIG 每条设置项追加 `scenes: ['edit','read','look','sys']` 标记（视图层元数据，不改键名） | `main.js` CONFIG 定义 | 低（纯新增字段） |
| S2 视图层 | 面板顶部渲染场景 Tab；按 Tab 过滤渲染子组（子组结构由 `scenes` 派生） | `PureHideSettingTab.display()` | 中 |
| S3 交互 | 搜索跨 Tab 联动、Tab/折叠状态记忆、滑块数值同步、单项恢复默认 | `filterSettings` + 渲染循环 | 中 |
| S4 回归 | 16 组视图回归 + 4 场景视图回归 + 旧 data.json 兼容 + 全命令验证 | — | — |

**与规划的关系**：本设计纳入 `IMPROVEMENT_PLAN.md` 作为"设置面板整合优化"的深化条目；实施前更新规划状态为"设计中"，实施后按门禁回归。未决事项（如场景归属的最终划分）登记至 OPEN-DECISIONS 后实施。
